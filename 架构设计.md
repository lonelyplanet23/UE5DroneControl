# 系统架构设计

> 版本：v0.4
> 日期：2026-05-23（补充预演关卡按注册表自动生成无人机 Actor 机制）

---

## 一、总体分层

```
┌─────────────────────────────────────────────────────────────────┐
│                        UE5 前端                                  │
│  ┌──────────┐  ┌──────────┐  ┌──────────┐  ┌────────────────┐  │
│  │ 阵列编辑  │  │无人机状态 │  │ 实时部署  │  │  镜头控制系统  │  │
│  └──────────┘  └──────────┘  └──────────┘  └────────────────┘  │
│  ┌──────────────────────────────────────────────────────────┐   │
│  │              UE 通讯模块（HTTP + WebSocket）               │   │
│  └──────────────────────────────────────────────────────────┘   │
└─────────────────────────────────────────────────────────────────┘
                              │  HTTP REST（端口 8080）
                              │  WebSocket（端口 8081，控制指令 + 遥测推送）
                              │  （UE5 连接后端局域网 IP，后端监听 0.0.0.0）
┌─────────────────────────────────────────────────────────────────┐
│                     后端服务（C++）                                │
│  ┌──────────┐  ┌──────────┐  ┌──────────┐  ┌────────────────┐  │
│  │ 注册管理  │  │ 队列管理  │  │ 执行引擎  │  │   通讯模块     │  │
│  └──────────┘  └──────────┘  └──────────┘  └────────────────┘  │
│  ┌──────────────────────────────────────────────────────────┐   │
│  │          坐标转换模块 + 多线程 UDP 发送（每机独立线程）     │   │
│  └──────────────────────────────────────────────────────────┘   │
└─────────────────────────────────────────────────────────────────┘
                              │  UDP 24字节控制包（≥2Hz 心跳，每机独立线程）
                              │  UDP YAML 遥测包（10Hz）
┌─────────────────────────────────────────────────────────────────┐
│                     PX4 无人机（×6 max）                          │
└─────────────────────────────────────────────────────────────────┘
```

---

## 二、UE5 前端职责

### 2.1 各模块职责

```
UE5 前端
│
├── 阵列编辑模块
│   ├── 航点编辑（俯视点击 + 高度调节）
│   ├── 执行模式配置（侦察/巡逻/攻击）
│   ├── 阵列 JSON 序列化/持久化（本地存储）
│   ├── 阵列预演动画（UE 内模拟）
│   └── 碰撞检测（UE 物理/几何检测）
│
├── 无人机状态模块
│   ├── 注册表单 UI（填写后 POST 到后端）
│   ├── 无人机列表渲染（轮询后端 GET）
│   └── 删除/编辑操作（PUT/DELETE 到后端）
│
├── 实时部署模块
│   ├── 影子机（Shadow Drone）
│   │   ├── 按指令轨迹移动（UE 端本地模拟）
│   │   └── 延迟计算（纯 UE 端计算）
│   ├── 镜像机（Mirror Drone）
│   │   ├── 接收后端推送的遥测数据（WebSocket）
|   |   ├── 确保坐标系轴向对齐
│   │   └── 驱动 Mesh 位置/姿态（UE 世界坐标，由后端已转换好）（需要测试）
│   ├── 实时控制模式
│   │   ├── 射线检测选中无人机
│   │   ├── 点击地图生成目标点（UE 世界坐标）
│   │   └── 发送控制指令到后端（WebSocket）
│   ├── 阵列执行模式
│   │   ├── 无人机槽位映射 UI
│   │   ├── 下发阵列任务到后端（HTTP POST）
│   │   ├── 集结阶段可视化
│   │   │   ├── 镜像机跟随遥测实时渲染（正常运行）
│   │   │   ├── 影子机隐藏或跟随镜像机同步移动（集结期间不独立运动）
│   │   │   └── 弹窗提示"集结中 N/M 架已就位"
│   │   └── 接收执行状态反馈（WebSocket：assembling / assembly_complete / executing）
│   └── 弹窗通知系统（统一调用入口，供所有模块使用）
│       ├── 告警弹窗（低电量 / 断联）
│       ├── 集结状态弹窗（集结中 / 就绪 / 超时）
│       └── 通用提示弹窗（操作反馈 / 错误提示）
│
├── 镜头控制系统
│   ├── 跟随镜头（45度俯视跟随指定无人机）
│   ├── 自由镜头（WASDQE + 鼠标朝向）
│   ├── F 键切换 + 平滑过渡
│   └── 跟随镜头下 Q/E 调节飞行高度
│
└── UE 通讯模块
    ├── HTTP Client（注册/查询/删除，2~5s 轮询）
    ├── 本地注册表缓存（Saved/DroneRegistry.json）
    └── WebSocket Client
        ├── 发送：控制指令（目标点、暂停、阵列任务）
        └── 接收：遥测推送（位置/姿态/电量/状态）
```

### 2.2 初始化位置同步（锚定流程）

```
UE5 端触发锚定：
  1. 后端推送"无人机上电"事件（WebSocket），事件直接携带 GPS 坐标（gps_lat/gps_lon/gps_alt）
  2. UE 收到 power_on 事件，调用 Cesium API 将 GPS 转换为 UE 世界坐标 → 设为镜像机锚点
  3. UE 设置影子机初始位置 = 镜像机位置 = 锚点位置
  4. 后续每帧：镜像机位置 = 锚点 + 后端推送的 UE 坐标偏移

断联重连：
  - 后端推送 reconnect 事件（同样携带新 GPS 坐标）→ UE 重新执行步骤 2~4
```

> 说明：power_on 和 reconnect 事件均直接携带 GPS 坐标，UE5 无需额外 HTTP GET /anchor。
> GET /anchor 接口保留，仅用于 UE5 重启后补偿查询已上电无人机的锚点。

### 2.3 无人机注册列表同步

后端注册表是权威数据源，UE5 本地维护一份注册列表缓存，用于重启后保留曾经注册过的无人机条目和 UI 状态。当前 UE5 实现为：

1. 用户在 `WBP_MainMenu` 注册无人机时，UE5 调用 `UDroneNetworkManager::RegisterDroneToBackend`，向后端发送 `POST /api/drones`。
2. 后端完成注册、分配数字 ID、绑定 `slot` 对应的 UDP 端口，并持久化到后端注册文件。
3. UE5 收到注册响应后，将后端返回的无人机写入 `UDroneRegistrySubsystem`，并持久化到 `Saved/DroneRegistry.json`。
4. UE5 运行期间定时轮询 `GET /api/drones`，用后端返回的注册表更新本地描述符、端口、`slot`、`topic_prefix` 和在线状态。
5. 如果某架无人机存在于 UE5 本地缓存，但本轮后端注册表没有返回该无人机，UE5 不删除该条目，而是将其状态标记为 `Lost Connection`，用于表示“曾经连接/注册过，但本次运行没有从后端同步到”。
6. WebSocket 遥测或事件到达时，UE5 用实时数据刷新对应无人机状态；后端状态为 `online/lost/offline` 时，轮询结果也会同步到本地状态。

该机制的目标是：后端负责真实注册和通讯端口绑定，UE5 负责显示完整的历史注册列表；即使后端重启、注册表清空或某架机暂未连接，UE5 仍能保留 UI 条目并明确显示失联状态。

### 2.4 预演关卡按注册表自动生成无人机 Actor

进入预演关卡（DroneOps）时，`ADroneOpsGameMode::BeginPlay` 调用 `SpawnReceiversFromRegistry()`，根据本地注册表中的所有 `FDroneDescriptor` 自动生成对应数量的 `ARealTimeDroneReceiver` 镜像机 Actor。

**生成流程：**

```
ADroneOpsGameMode::BeginPlay()
  → InitializeCoordinateService()     初始化 Cesium 或 Simple 坐标服务
  → InitializeDroneRegistry()         （Registry 已在 GameInstance 初始化时从磁盘加载）
  → SpawnReceiversFromRegistry()
      for each FDroneDescriptor in Registry（按 Slot/DroneId 升序）:
        ├─ 如果场景中已存在该 DroneId 的 Actor → 调 ApplyDescriptor 更新身份和状态
        └─ 否则 SpawnActorDeferred<ARealTimeDroneReceiver>
               位置 = ReceiverSpawnOrigin + (SpawnIndex × ReceiverSpawnSpacingCm, 0, 0)
               → ApplyDescriptor(Desc, InitialAvailability)
               → FinishSpawning() → 触发 BeginPlay
```

**生成位置：** `ReceiverSpawnOrigin`（默认 `Z=3000cm` 空中3米）起，按注册顺序横向排列，间距 `ReceiverSpawnSpacingCm`（默认400cm）。两个参数均可在 `ADroneOpsGameMode` 蓝图子类中配置。

**初始状态确定：**

| 磁盘保存状态 | 进关卡初始状态 | 说明 |
|------|------|------|
| `online` | `Lost`（降级） | 本次运行尚未经后端或遥测验证 |
| `lost` | `Lost` | 保持 |
| `offline` | `Offline` | 保持 |

**断联无人机的行为：**
- 断联（`Lost`）的无人机和在线无人机使用相同逻辑生成 Actor，生成在 SpawnOrigin 附近静止不动
- 中键信息面板显示"失联"，侧边列表显示橙色"失联"
- 收到 `reconnect` 事件并获得新 GPS 锚点后，Actor 自动重新定位并切换为"连接"状态

**状态唯一数据源：**

`UDroneRegistrySubsystem` 是整个项目的唯一状态中心，以下模块全部读写同一张表，不存在独立的第二份状态：

| 模块 | 操作 |
|------|------|
| 主菜单注册 UI | 写入描述符 + 后端 POST |
| `SpawnReceiversFromRegistry` | 读取描述符 + 初始 Availability |
| HUD 侧边 `DroneListWidget` | 读取描述符 + Availability，定时刷新 |
| 中键信息面板 | 读取遥测快照（含 Availability） |
| `PlayerController` 控制指令 | 读取 DroneId / CommandMode |
| `DroneNetworkManager` HTTP 轮询 | 写入描述符 + Availability |
| WebSocket telemetry 推送 | 写入遥测快照（含位置/姿态/状态） |

**状态传播路径（运行时）：**

```
① 后端 HTTP 轮询（3s）
     → SyncDroneListToRegistry → Registry::MarkDroneAvailability
     → OnTelemetryUpdated 广播 → UI 刷新

② WebSocket telemetry 推送
     → Registry::UpdateTelemetry（位置/姿态/Availability）
     → OnTelemetryUpdated 广播
     → RealTimeDroneReceiver::OnWebSocketTelemetry → 镜像机移动

③ DroneTelemetryComponent 超时（仅 UDP 模式，3s无数据）
     → Availability 标记为 Lost
     → PropagateToRegistry → Registry::UpdateTelemetry

④ WebSocket event: lost_connection
     → Registry::MarkDroneAvailability(Lost) → 磁盘持久化
```

---

## 三、后端职责

### 3.1 各模块职责

```
后端服务
│
├── 注册管理模块
│   ├── 无人机注册（持久化：名称/ID/型号/IP:Port/视频流地址）
│   ├── 无人机删除/更新
│   └── ID ↔ 编号映射管理（ID 由后端自动分配，编号固定对应通讯端口）
│
├── 队列管理模块
│   ├── 每架无人机独立指令队列
│   ├── 心跳维持（≥2Hz 向 PX4 发送保活包，防止退出 Offboard 模式）
│   ├── 连接状态机（在线/离线/连接中/失联）
│   └── 超时检测（默认 10s 无心跳 → 失联）
│
├── 执行引擎（核心业务逻辑）
│   ├── 集结流程（阵列执行前置步骤）
│   │   ├── 接收 POST /api/arrays 后进入 ASSEMBLING 状态
│   │   ├── 为每架无人机计算初始槽位目标点（NED）
│   │   ├── 实时避障：监控所有机当前位置，动态调整集结路径防止碰撞
│   │   ├── 判断每架无人机到达初始槽位（误差 < 阈值）
│   │   ├── 全部到位后进入 READY 状态，推送 assembly_complete 事件给 UE5
│   │   └── 集结超时（默认 60s）则推送 assembly_timeout 告警
│   ├── 侦察模式：按航点序列依次发送目标点，到达后发下一个
│   ├── 巡逻模式：按航点序列飞行 + 目标识别事件触发脱离序列
│   ├── 攻击模式：经过中间航点去攻击目标点
│   ├── 阵列任务调度（多机并发执行，各自独立队列）
│   ├── 暂停/恢复控制（P 键指令处理）
│   └── 实时避障算法（集结阶段 + 实时控制阶段均启用，基于所有机当前遥测位置）
│
├── 通讯模块
│   ├── 对 UE 端
│   │   ├── HTTP REST Server（注册/查询/删除接口）
│   │   └── WebSocket Server
│   │       ├── 接收：控制指令（目标点/暂停/阵列任务）
│   │       └── 推送：遥测数据（已转换为 UE 世界坐标）、状态事件、告警
│   └── 对无人机端
│       ├── UDP 发送（24字节控制包，≥2Hz 心跳）
│       └── UDP 接收（YAML 遥测包，10Hz）
│
└── 坐标转换模块
    ├── NED → UE 偏移量转换（×100，Z 取反）
    ├── UE 偏移 → NED 控制坐标（×0.01，Z 取反）（UE5 端已预先减去锚点，后端只做单位转换）
    ├── 四元数重排与 Yaw 翻转
    └── GPS 锚点管理（上电时记录，断联重连时更新，通过 WebSocket 事件推送给 UE5）
```

### 3.2 后端 HTTP REST 接口

| 接口 | 方法 | 说明 | 调用方 |
|------|------|------|--------|
| `/api/drones` | GET | 获取所有注册无人机列表+状态 | UE 轮询（2~5s） |
| `/api/drones` | POST | 注册新无人机 | UE 注册表单提交 |
| `/api/drones/{id}` | PUT | 更新无人机信息 | UE 编辑表单提交 |
| `/api/drones/{id}` | DELETE | 删除无人机 | UE 删除操作 |
| `/api/drones/{id}/anchor` | GET | 获取无人机 GPS 锚点坐标 | UE 锚定流程 |
| `/api/arrays` | POST | 下发阵列任务 | UE 阵列执行模式 |
| `/api/arrays/{id}/stop` | POST | 停止阵列任务 | UE 停止操作 |

### 3.3 WebSocket 消息类型

**UE → 后端（控制指令）**

```json
// 目标点指令（x/y/z 为相对锚点的 UE 坐标偏移，厘米）
{ "mode": "move", "drone_id": "d1", "x": 1000.0, "y": 2000.0, "z": 500.0 }

// 暂停/恢复
{ "type": "pause", "drone_ids": ["d1", "d2"] }
{ "type": "resume", "drone_ids": ["d1", "d2"] }
```

**后端 → UE（推送）**

```json
// 遥测数据（已转换为 UE 坐标偏移，单位：厘米；speed 为标量 m/s；battery=-1 表示不可用）
{ "type": "telemetry", "drone_id": "d1", "x": 10000, "y": 20000, "z": 5000,
  "yaw": 45.0, "pitch": 0.0, "roll": 0.0, "speed": 3.5, "battery": 85 }

// 状态事件（power_on 和 reconnect 必须携带 GPS 坐标）
{ "type": "event", "drone_id": "d1", "event": "power_on", "gps_lat": 39.9, "gps_lon": 116.3, "gps_alt": 50.0 }
{ "type": "event", "drone_id": "d1", "event": "reconnect", "gps_lat": 39.9, "gps_lon": 116.3, "gps_alt": 50.0 }
{ "type": "event", "drone_id": "d1", "event": "lost_connection" }

// 集结进度
{ "type": "assembling", "array_id": "a1", "ready_count": 2, "total_count": 4 }
{ "type": "assembly_complete", "array_id": "a1" }
{ "type": "assembly_timeout", "array_id": "a1", "ready_count": 2, "total_count": 4 }

// 告警
{ "type": "alert", "drone_id": "d1", "alert": "low_battery", "value": 18 }
{ "type": "alert", "drone_id": "d1", "alert": "lost_connection" }
```

---

## 四、前后端交互时序

### 4.1 无人机上电锚定

```
UE5                    后端（C++）              PX4无人机
 │                      │                         │
 │──── POST /api/drones ─→│                         │
 │  注册slot/ip信息        │  持久化注册表/绑定端口   │
 │←── 注册响应id/slot ────│                         │
 │  写入UE本地注册表       │                         │
 │                      │←── UDP遥测（首包）────────│
 │                      │  记录GPS锚点              │
 │←── WS: power_on ────│  （携带gps_lat/lon/alt）  │
 │  Cesium转UE世界坐标   │                         │
 │  设置AnchorWorldLoc   │                         │
 │                      │←── UDP遥测（持续）────────│
 │←── WS: telemetry ───│  NED→UE偏移转换           │
 │  镜像机 = 锚点+偏移   │                         │
```

### 4.2 实时控制派发目标点

```
UE5                    后端（C++）              PX4无人机
 │                      │                         │
 │  用户点击地图          │                         │
 │  target - Anchor      │                         │
 │──── WS: move ────────→│                         │
 │                      │  UE偏移→NED转换           │
 │                      │──── UDP 24字节 ──────────→│
 │                      │  心跳（≥2Hz，独立线程）    │
 │                      │──── UDP 24字节 ──────────→│
 │←── WS: telemetry ───│←── UDP遥测 ─────────────│
 │  更新镜像机位置        │                         │
```

### 4.3 阵列任务执行

```
UE5                    后端（C++）              PX4无人机
 │                      │                         │
 │  用户配置路径并确认    │                         │
 │──── POST /arrays ─────→│                         │
 │                      │  进入 ASSEMBLING 状态     │
 │←── WS: assembling ───│  计算各机初始槽位          │
 │  影子机隐藏/跟随镜像机 │  实时避障路径规划          │
 │  弹窗"集结中 0/N"     │──── UDP 集结指令 ─────────→│
 │                      │←── UDP 遥测（持续）────────│
 │←── WS: telemetry ────│  判断各机是否到位          │
 │  镜像机实时渲染        │  到位计数 +1              │
 │←── WS: assembling ───│  推送进度（已到位 k/N）    │
 │  弹窗更新"集结中 k/N" │                         │
 │                      │  全部到位                 │
 │←── WS: assembly_complete│  进入 EXECUTING 状态   │
 │  影子机恢复独立运动    │  按模式逐航点发送指令      │
 │  弹窗"集结完成，执行中"│──── UDP 执行指令 ─────────→│
 │←── WS: telemetry ────│←── UDP 遥测 ─────────────│
 │  更新影子机/镜像机     │                         │
```

### 4.4 断联重连

```
UE5                    后端（C++）              PX4无人机
 │                      │  10s无遥测               │
 │←── WS: event ───────│  event=lost_connection   │
 │←── WS: alert ───────│  alert=lost_connection   │
 │  显示断联告警弹窗      │                         │
 │                      │←── UDP遥测（重连首包）────│
 │                      │  记录新GPS锚点            │
 │←── WS: event ───────│  event=reconnect          │
 │                      │  （携带新gps_lat/lon/alt）│
 │  重新Cesium转换       │                         │
 │  更新AnchorWorldLoc   │                         │
```

---

## 五、职责边界速查

| 功能 | UE5 前端 | 后端 |
|------|----------|------|
| 无人机注册表单 UI | ✓ | |
| 注册数据持久化 | ✓（本地缓存/历史列表） | ✓（权威注册表） |
| 注册列表同步 | ✓（POST 注册 + GET 轮询 + 本地失联标记） | ✓（注册响应 + 状态返回） |
| ID 分配与编号映射 | | ✓ |
| 心跳维持（≥2Hz） | | ✓ |
| 连接状态机 | | ✓ |
| 侦察/巡逻/攻击逻辑 | | ✓ |
| 集结流程（飞到初始槽位） | | ✓ |
| 实时避障（集结 + 执行阶段） | | ✓ |
| 预演碰撞检测（静态几何） | ✓ | |
| NED ↔ UE 坐标转换 | | ✓ |
| GPS → UE 世界坐标（Cesium） | ✓ | |
| GPS 锚点记录 | | ✓ |
| 影子机插值移动 | ✓ | |
| 影子机集结期间隐藏/跟随镜像机 | ✓ | |
| 镜像机位置驱动 | ✓ | |
| 影子机/镜像机延时计算 | ✓ | |
| 阵列 JSON 本地存储 | ✓ | |
| 阵列预演动画 | ✓ | |
| 弹窗通知系统（统一入口） | ✓ | |
| 集结进度推送 | | ✓ |
| 镜头控制 | ✓ | |
| 告警弹窗 UI | ✓ | |
| 告警触发判断 | | ✓ |
| 电量数据来源 | | ✓（遥测） |

---

## 六、人员分工（五周开发计划）

### 后端团队（3人）

| 人员 | 主要职责 | 核心模块 |
|------|----------|----------|
| **李昊泽** | 项目经理 + 通讯模块 | HTTP REST Server、WebSocket Server、遥测推送、状态事件推送、告警推送；提供 Mock Server 和测试脚本 |
| **陈锦垚** | 底层通信 + 坐标转换 | UDP 收发、坐标转换模块、连接状态机、队列管理、心跳维持、集结流程 |
| **关皓元** | 注册管理 + 执行引擎 | 无人机注册/删除/更新、ID↔编号映射、侦察/巡逻/攻击模式逻辑、阵列任务调度、实时避障 |

### UE5 前端团队（3人）

| 人员 | 主要职责 | 核心模块 |
|------|----------|----------|
| **杨璞** | 通讯模块负责人 | UE 通讯模块（HTTP Client + WebSocket Client）、地图交互（Cesium 点击/射线检测）、实时控制模式、多机选中 |
| **刘哲晗** | UI 负责人 + 测试负责人 | UI 设计（无人机状态列表、信息弹窗、告警弹窗、注册表单）、弹窗通知系统、全流程测试（Apifox） |
| **柯垣丞** | 阵列系统负责人 | 初始化位置同步（锚定流程）、阵列编辑模块（航点编辑、模式配置、预演动画、碰撞检测）、阵列执行模式（映射 UI + 影子机逻辑）、Cesium GPS 转换 |

### 开发周期（5周完成 + 1周冗余）

| 周次 | 目标 | 关键交付物 |
|------|------|-----------|
| **第一周** | 基础架构搭建 + 学习期 | 技术选型、环境搭建、基础框架、Mock Server |
| **第二周** | 核心通信链路打通 | HTTP 接口、WebSocket 连接、UDP 收发、坐标转换、GPS 锚定 |
| **第三周** | 实时控制 + 状态管理 | 实时控制模式、连接状态机、弹窗系统、move/pause/resume 指令 |
| **第四周** | 阵列执行 + 集结流程 | 阵列编辑、集结流程、三种执行模式、多机并发调度 |
| **第五周** | 完善功能 + 集成测试 | 阵列预演、碰撞检测、实时避障、告警系统、全流程测试 |
| **第六周** | 冗余周 | Bug 修复、性能优化、文档完善、演示准备 |

### 测试工具

- **Apifox**：HTTP REST + WebSocket 测试（全员）
- **Wireshark + UDPdump**：UDP 包格式验证（后端）
- **Mock Server**：Python Flask + websockets（李昊泽提供）

---

*架构设计 v0.3，依据接口与通讯数据规范 v1.0 更新，并补充 UE 注册同步与本地持久化机制。后端语言确认为 C++，旧 UDP 直连链路已弃用。*

